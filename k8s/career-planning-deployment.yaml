apiVersion: apps/v1
kind: Deployment
metadata:
  name: career-planning-service
  labels:
    app: career-planning-service
    microservice: career-planning
spec:
  replicas: 1
  selector:
    matchLabels:
      app: career-planning-service
  template:
    metadata:
      labels:
        app: career-planning-service
        microservice: career-planning
    spec:
      containers:
      - name: career-planning
        image: careercoach-backend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "career_growth_app.settings"
        - name: SERVICE_NAME
          value: "career-planning"
        command: ["/bin/sh"]
        args:
          - -c
          - |
            python manage.py migrate --noinput
            gunicorn --bind 0.0.0.0:8000 career_growth_app.wsgi:application
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"

